[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "factory"
dynamic = ["version"]
description = "Label-driven autonomous ticket pipeline: triage, worker agents in git worktrees, a deterministic gate, LLM review, and a serialized merge stage — for any GitHub repo."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = ["tomlkit>=0.13,<1"]
keywords = ["agents", "github", "automation", "ci", "worktree"]
classifiers = [
  "Environment :: Console",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Topic :: Software Development :: Build Tools",
]

[project.optional-dependencies]
atlas = ["graphifyy==0.9.56"]

[project.urls]
Homepage = "https://github.com/mikeroySoft/factory"

[project.scripts]
factory = "factory.cli:main"

[tool.hatch.version]
path = "factory/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["factory", "agent_factory"]

[tool.ruff]
line-length = 100
target-version = "py311"
